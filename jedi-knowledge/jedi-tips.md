# JEDI tips

Team-curated tips, commands, and gotchas for working with the JEDI bundle and
surrounding tools. Add anything you find yourself doing repeatedly, anything
that took you longer than it should have, or anything a new teammate would
benefit from knowing.

This file is tracked. Open a PR to contribute.

---

## Build & environment

- *(add tips here)*

## Running tests

- *(add tips here)*

## Debugging

- *(add tips here)*

## Common gotchas

- `jedi-tools` requires `git-lfs` to be installed and enabled before cloning.
  Run `git lfs install` once per machine.
- The `mpas` CMakeLists pin is a commit on `release-v8.4.0`, not a tag —
  see `jedi-knowledge/mpas.md` for context.

## Useful commands

- *(add commands here)*

## Workflow recipes

- *(add recipes here)*
