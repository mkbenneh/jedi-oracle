# Cloned codebases — the current layout puts these alongside jedi-oracle
# (../jedi-bundle etc.), but legacy checkouts cloned them in here. Kept
# ignored so legacy layouts never leak into commits.
jedi-bundle/
jedi-docs/
jedi-tools/
jedi-workflow/

# Build artifacts (legacy in-repo build trees)
build/

# Runtime files written by skills
whatHasChanged.md
externalKnowledge.md
jedi-knowledge/external/
investigations/
jedi-knowledge/project-notes/

# Editor / OS
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
